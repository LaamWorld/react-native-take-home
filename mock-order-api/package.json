{
	"name": "mock-order-api",
	"version": "1.0.0",
	"scripts": {
		"start": "json-server db.json --port 4000 --routes routes.json --middlewares middleware.js"
	},
	"devDependencies": {
		"json-server": "^0.17.4"
	}
}
